project: summit
python: python3.4
repository: "https://github.com/pywaw/summit.pywaw.org.git"
django_settings: "{{ project }}.settings.{{ settings }}"
app_path: "/home/app/apps/{{ project }}_{{ settings }}"
virtualenv_path: "{{ app_path }}/venv"
repository_path: "{{ app_path }}/repo"
db_user: "{{ project }}"
db_name: "{{ project }}_{{ settings }}"
