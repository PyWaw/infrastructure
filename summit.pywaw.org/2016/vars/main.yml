project: summit_2016
django_project: summit
python: python3.5
repository: "https://github.com/pywaw/2016.summit.pywaw.org.git"
django_settings: "{{ django_project }}.settings.{{ settings }}"
app_path: "/home/app/apps/{{ project }}_{{ settings }}"
virtualenv_path: "{{ app_path }}/venv"
repository_path: "{{ app_path }}/repo"
db_user: "{{ project }}"
db_name: "{{ project }}_{{ settings }}"
